# Identity

DEADGL is not a tiny OBJ renderer course.

It is a graphics command machine.

Input is a command stream. Output is pixels. Proof is a hash. The code does not climb toward a fake engine. It stays close to memory.

No OBJ-first pipeline. No lesson ladder. No hidden renderer class stack. No shader costume.

A command changes the framebuffer now.
